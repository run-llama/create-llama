{
	"name": "create-llama-monorepo",
	"version": "1.0.0",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "pnpm -r --filter create-llama build",
		"dev": "pnpm -r --filter create-llama dev"
	}
}
